%td.defendants
  = b.label do
    #{claim.defendants.map(&:name).join(', ')}
%td.case_number
  = b.label do
    #{claim.case_number}
%td.advocate
  = b.label do
    #{claim.advocate.email}
%td.court_name
  = b.label do
    #{claim.court.name}
%td
  = b.check_box