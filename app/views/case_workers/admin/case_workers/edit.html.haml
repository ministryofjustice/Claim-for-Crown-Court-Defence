= content_for :page_title, flush: true do
  = t('.page_title', caseworker: @case_worker.name)

= render partial: 'layouts/page_caption', locals: { page_heading: t('.page_heading') }

.govuk-grid-row
  .govuk-grid-column-two-thirds
    = render 'form'
