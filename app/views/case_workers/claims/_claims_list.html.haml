.container
  = render partial: 'search_form', locals: { archived: params[:action] == 'archived' ? true : false }

#search-summary
  - if params[:search].present?
    = render partial: 'shared/search_summary'

- if @claims.none?
  %p
    = t("shared.no_claims_found")
- else
  = render partial: 'claims'
