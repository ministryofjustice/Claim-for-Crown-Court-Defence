%h1.page-title
  = "Claims"

#report-options.container
  = render partial: 'search_form'

#search-summary
  - if params[:search].present?
    = render partial: 'shared/search_summary'

#list-controls
  = render partial: 'list_controls'

- if @claims.none?
  %p
    No claims found
- else
  = render partial: 'caseworker_claims'
