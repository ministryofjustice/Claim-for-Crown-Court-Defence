= content_for :page_title, flush: true do
  = t('.page_title')

- present(@claim) do |claim|
  = render 'errors/error_summary', ep: @claim, error_summary: t('.error_summary')
  = render 'errors/injection_error_summary', ep: claim
  = render 'warnings/injection_warnings_summary', claim: claim

  %p.govuk_body
    = beta_test_link
  = render partial: beta_test_partial('shared/claim_header'), locals: { claim: claim }
  = render partial: beta_test_partial('shared/new_claim_accordion'), locals: { claim: claim, status_disabled: false }
