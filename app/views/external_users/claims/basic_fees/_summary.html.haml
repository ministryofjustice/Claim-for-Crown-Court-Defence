- unless claim.fixed_fee_case?
  .form-section
    = render partial: 'external_users/claims/summary_fees', locals: { claim: claim, header: t('external_users.claims.basic_fees.summary.header'), collection: claim.basic_fees }
