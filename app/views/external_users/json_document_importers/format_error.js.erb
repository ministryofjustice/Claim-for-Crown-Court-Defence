$('#importer').find('form')
  .find('.file-upload-name')
    .empty();

$('#importer')
  .addClass('has-errors')
  .find('.errors')
    .html("<em><%= filename %></em> is either not JSON or contains errors. Choose another file.");

$('.importer-results').empty();
$('input.btn-import-file').prop('disabled', false).val('Import claim(s)').prop('aria-hidden', false).hide();
