= render partial: 'error_summary', locals: { ep: @error_presenter }

= render partial: 'layouts/header', locals: {page_heading: t('page_headings.edit_lgfs_claim')}

= render partial: 'external_users/claims/form_layout_wrapper', locals: { claim: @claim, display_buttons: false }
