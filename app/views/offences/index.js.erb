<% if params[:description] =~ /-- Select offence --/ %>
  $('.offence-class-select').html('')
  $('.offence-class-select').hide();
<% else %>
  $('.offence-class-select').html('<%=j render partial: "external_users/claims/offence_select", locals: { offences: @offences } %>');
  $('.offence-class-select select').select2({
    matcher: moj.Modules.Select2.startOfMatcher
  });
  moj.Modules.NewClaim.attachToOffenceClassSelect();
  $('.offence-class-select').slideDown();
<% end %>
