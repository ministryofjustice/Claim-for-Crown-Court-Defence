.grid-row.history
  .column-one-third
    = ''
  .column-two-thirds
    .message-right.event
      .message-container
        .message-body
          = determination.event
          %table
            %caption.visuallyhidden
              = t('.caption')
            - determination.itemise do |item, amount|
              %tr
                %th{ scope: 'row' }
                  #{item}:
                %td
                  #{number_to_currency(amount)}
