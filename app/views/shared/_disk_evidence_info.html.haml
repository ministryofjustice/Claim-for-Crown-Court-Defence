%h3.heading-small
  Help with sending the evidence
  = t('disk_evidence.help_sending_evidence')

%p
  = t('disk_evidence.hint')

%p
  = t('disk_evidence.hint_html')

= render partial: 'external_users/claims/supporting_documents/laa_address', locals: { f: f }
