#miscellaneous-fees-section.form-section
  = render partial: 'shared/summary/summary_fees', locals: { claim: claim, header: t('external_users.claims.misc_fees.summary.header'), collection: claim.misc_fees, editable: editable, section: section, step: :miscellaneous_fees }

  - if claim.requires_interim_claim_info?
    = render partial: 'shared/summary/interim_claim_info/summary', locals: { claim: claim }
