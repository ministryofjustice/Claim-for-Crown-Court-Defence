apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: cccd-app-ingress
  namespace: cccd-dev-agfs
spec:
  rules:
    - host: dev-agfs.claim-crown-court-defence.service.justice.gov.uk
      http:
        paths:
          - path: /
            backend:
              serviceName: cccd-app-service
              servicePort: 80
  tls:
    - hosts:
      - dev-agfs.claim-crown-court-defence.service.justice.gov.uk
      secretName: cccd-dev-agfs-cert
